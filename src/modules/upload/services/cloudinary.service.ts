// modules/upload/services/cloudinary.service.ts
import { injectable } from "tsyringe";

@injectable()
export class CloudinaryService {
  /**
   * Sube una imagen a Cloudinary.
   * @param file Buffer o ruta del archivo.
   * @param folder Carpeta en Cloudinary donde se guardará el archivo.
   * @returns {public_id, secure_url}
   */
}
